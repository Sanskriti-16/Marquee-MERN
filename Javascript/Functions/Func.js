console.log(greetMsg);
let greetMsg = greet('Sanskriti')
function greet(name) {
    return `Hello, $(name)`;
}
function execute(fn, name){
    fn(name);
}
function execute(fn, name){
    fn(name);
}
execute(sayHello);